# Отчёт об адаптации изображений под соотношение 4:3

**Дата:** 13 марта 2026 г.
**Статус:** ✅ Выполнено

---

## 📋 Изменения

### 1. Hero секция (Главное фото)

**Файл:** `index.php` (строки 354-362)

**Изменения:**
- **sizes атрибут:** Обновлён для корректного отображения на разных экранах
  - Было: `(max-width: 480px) 210px, (max-width: 768px) 280px, 480px`
  - Стало: `(max-width: 480px) 280px, (max-width: 768px) 400px, 640px`
- **Version:** Обновлён с `v=3` до `v=4` (для сброса кэша)
- **Размеры изображения:** `640x480` (4:3)

**Файл:** `styles.css` (строки 492-512)

**Изменения:**
- `.image-container`: `700x550px` (было `700x700px`)
- `.image-frame`: `640x480px` (было `480x480px`)
- `aspect-ratio`: `4 / 3` (было `1 / 1`)

---

### 2. Секция "О тренере"

**Файл:** `index.php` (строки 381-391)

**Изменения:**
- **sizes атрибут:** Обновлён для корректного отображения
  - Было: `(max-width: 480px) 210px, (max-width: 768px) 280px, 400px`
  - Стало: `(max-width: 480px) 280px, (max-width: 768px) 355px, 532px`
- **Version:** Обновлён с `v=4` до `v=5` (для сброса кэша)
- **Размеры изображения:** `532x400` (4:3)
- **Исправлена ошибка:** В srcset для PNG было `320px` вместо `320w`

**Файл:** `styles.css` (строки 711-720)

**Изменения:**
- `max-width`: `532px` (было `400px`)
- `aspect-ratio`: `4/3` (было `4/5`)

---

### 3. Минификация CSS

**Файлы:**
- `styles.min.css` (обновлён, версия 2.2)
- `responsive.min.css` (обновлён, версия 2.2)

**Команда сборки:**
```bash
npm run build
```

---

## 📊 Итоговые соотношения сторон

| Секция | Ширина | Высота | Соотношение |
|--------|--------|--------|-------------|
| **Hero** | 640px | 480px | **4:3** ✅ |
| **О тренере** | 532px | 400px | **4:3** ✅ |

---

## 🎨 Адаптивность

### Мобильные устройства (< 480px)
- Hero: **280x210px** (4:3)
- О тренере: **280x210px** (4:3)

### Планшеты (480-768px)
- Hero: **400x300px** (4:3)
- О тренере: **355x266px** (4:3)

### Десктоп (> 768px)
- Hero: **640x480px** (4:3)
- О тренере: **532x400px** (4:3)

---

## ✅ Проверка

### До изменений:
- ❌ Hero: 1:1 (квадрат)
- ❌ О тренере: 4:5 (вертикальное)

### После изменений:
- ✅ Hero: **4:3** (горизонтальное)
- ✅ О тренере: **4:3** (горизонтальное)

---

## 🚀 Развёртывание

### Локально:
```bash
# Перезапустить контейнеры
docker-compose restart php web

# Очистить кэш браузера
Ctrl + Shift + R (Windows)
Cmd + Shift + R (Mac)
```

### На сервере:
```bash
# Отправить изменения
git add .
git commit -m "feat: адаптация изображений под соотношение 4:3"
git push

# На сервере
ssh root@85.198.64.110
cd ~/zumba-site
git pull
docker-compose restart php web
```

---

## 📝 Примечания

1. **Кэширование:** Version параметр (`?v=4`, `?v=5`) обеспечивает сброс кэша браузером
2. **Object-fit:** Изображения используют `object-fit: cover` для корректного масштабирования
3. **Object-position:** Центрирование по `center center`
4. **Srcset:** Автоматический выбор размера в зависимости от экрана

---

## 🖼️ Исходные файлы

Для создания изображений 4:3 используйте оригинальное фото:
- `photo_2026-03-09_22-52-09.jpg`

Рекомендуемые размеры для экспорта:
- **640x480** (Hero, десктоп)
- **532x400** (О тренере, десктоп)
- **480x360**, **400x300**, **355x266**, **320x240**, **280x210** (адаптивные версии)

---

**Исполнитель:** AI Assistant
**Версия:** 1.0
